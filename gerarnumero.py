from random import randint, random


import random

aposta = []
            

def gerandonumero6(aposta):
    while len(aposta) <=5:
        aposta.append(random.randint(1,60))

    apostafinal = []

    numeros_unico = set(aposta)

    for num in numeros_unico:
        apostafinal.append(str(num))

    if len(apostafinal) == 6:
        apostafinal.sort()
        return apostafinal
    else:
        while len(apostafinal) <= 5:
            aposta.append(random.randint(1,60))

            apostafinal = []

            numeros_unico = set(aposta)

            for num in numeros_unico:
                apostafinal.append(str(num))
                
        apostafinal.sort()
        return apostafinal

def gerandonumero7(aposta):
    while len(aposta) <=6:
        aposta.append(random.randint(1,60))

    apostafinal = []

    numeros_unico = set(aposta)

    for num in numeros_unico:
        apostafinal.append(str(num))

    if len(apostafinal) == 7:
        apostafinal.sort()
        return apostafinal
    else:
        while len(apostafinal) <= 6:
            aposta.append(random.randint(1,60))

            apostafinal = []

            numeros_unico = set(aposta)

            for num in numeros_unico:
                apostafinal.append(str(num))
                
        apostafinal.sort()
        return apostafinal

def gerandonumero8(aposta):
    while len(aposta) <=7:
        aposta.append(random.randint(1,60))

    apostafinal = []

    numeros_unico = set(aposta)

    for num in numeros_unico:
        apostafinal.append(str(num))

    if len(apostafinal) == 8:
        apostafinal.sort()
        return apostafinal
    else:
        while len(apostafinal) <= 7:
            aposta.append(random.randint(1,60))

            apostafinal = []

            numeros_unico = set(aposta)

            for num in numeros_unico:
                apostafinal.append(str(num))
                
        apostafinal.sort()
        return apostafinal

def gerandonumero9(aposta):
    while len(aposta) <=8:
        aposta.append(random.randint(1,60))

    apostafinal = []

    numeros_unico = set(aposta)

    for num in numeros_unico:
        apostafinal.append(str(num))

    if len(apostafinal) == 9:
        apostafinal.sort()
        return apostafinal
    else:
        while len(apostafinal) <= 8:
            aposta.append(random.randint(1,60))

            apostafinal = []

            numeros_unico = set(aposta)

            for num in numeros_unico:
                apostafinal.append(str(num))
                
        apostafinal.sort()
        return apostafinal

def gerandonumero10(aposta):
    while len(aposta) <=9:
        aposta.append(random.randint(1,60))

    apostafinal = []

    numeros_unico = set(aposta)

    for num in numeros_unico:
        apostafinal.append(str(num))

    if len(apostafinal) == 10:
        apostafinal.sort()
        return apostafinal
    else:
        while len(apostafinal) <= 9:
            aposta.append(random.randint(1,60))

            apostafinal = []

            numeros_unico = set(aposta)

            for num in numeros_unico:
                apostafinal.append(str(num))
                
        apostafinal.sort()
        return apostafinal

def gerandonumero11(aposta):
    while len(aposta) <=10:
        aposta.append(random.randint(1,60))

    apostafinal = []

    numeros_unico = set(aposta)

    for num in numeros_unico:
        apostafinal.append(str(num))

    if len(apostafinal) == 11:
        apostafinal.sort()
        return apostafinal
    else:
        while len(apostafinal) <= 10:
            aposta.append(random.randint(1,60))

            apostafinal = []

            numeros_unico = set(aposta)

            for num in numeros_unico:
                apostafinal.append(str(num))
                
        apostafinal.sort()
        return apostafinal
    
def gerandonumero12(aposta):
    while len(aposta) <=11:
        aposta.append(random.randint(1,60))

    apostafinal = []

    numeros_unico = set(aposta)

    for num in numeros_unico:
        apostafinal.append(str(num))

    if len(apostafinal) == 12:
        apostafinal.sort()
        return apostafinal
    else:
        while len(apostafinal) <= 11:
            aposta.append(random.randint(1,60))

            apostafinal = []

            numeros_unico = set(aposta)

            for num in numeros_unico:
                apostafinal.append(str(num))
                
        apostafinal.sort()
        return apostafinal

def gerandonumero13(aposta):
    while len(aposta) <=12:
        aposta.append(random.randint(1,60))

    apostafinal = []

    numeros_unico = set(aposta)

    for num in numeros_unico:
        apostafinal.append(str(num))

    if len(apostafinal) == 13:
        apostafinal.sort()
        return apostafinal
    else:
        while len(apostafinal) <= 12:
            aposta.append(random.randint(1,60))

            apostafinal = []

            numeros_unico = set(aposta)

            for num in numeros_unico:
                apostafinal.append(str(num))
                
        apostafinal.sort()
        return apostafinal

def gerandonumero14(aposta):
    while len(aposta) <=13:
        aposta.append(random.randint(1,60))

    apostafinal = []

    numeros_unico = set(aposta)

    for num in numeros_unico:
        apostafinal.append(str(num))

    if len(apostafinal) == 14:
        apostafinal.sort()
        return apostafinal
    else:
        while len(apostafinal) <= 13:
            aposta.append(random.randint(1,60))

            apostafinal = []

            numeros_unico = set(aposta)

            for num in numeros_unico:
                apostafinal.append(str(num))
                
        apostafinal.sort()
        return apostafinal

def gerandonumero15(aposta):
    while len(aposta) <=14:
        aposta.append(random.randint(1,60))

    apostafinal = []

    numeros_unico = set(aposta)

    for num in numeros_unico:
        apostafinal.append(str(num))

    if len(apostafinal) == 15:
        apostafinal.sort()
        return apostafinal
    else:
        while len(apostafinal) <= 14:
            aposta.append(random.randint(1,60))

            apostafinal = []

            numeros_unico = set(aposta)

            for num in numeros_unico:
                apostafinal.append(str(num))
                
        apostafinal.sort()
        return apostafinal

